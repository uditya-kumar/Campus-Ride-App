// Maximum days into the future a ride departure can be set or filtered to.
export const MAX_DEPARTURE_DAYS_AHEAD = 60;

// Upper bounds for ride form inputs — campus rides, not commercial fleets.
export const MAX_RIDE_SEATS = 10;
export const MAX_TOTAL_COST = 10_000;
